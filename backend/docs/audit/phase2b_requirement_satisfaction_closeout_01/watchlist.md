# Watchlist

- **Admin browser harness:** wire Playwright admin session the same way as production admin login (cookie/localStorage contract) so `admin_client_panel.png` captures the control panel UI, not marketing redirect.
- **Top-level `semantic_state` mirror:** authority blob carries `primary_evidence_record_id` for Legionella while row-level `semantic_state` may remain `MISSING` until CER verification policy promotes — cosmetic only; satisfaction truth converges.
- **Admin vs client API row counts:** admin enrich loads full DB portfolio; client API may filter — expect count deltas; use split field semantics not raw totals for parity checks.
