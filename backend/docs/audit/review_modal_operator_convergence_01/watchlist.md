# Watchlist — review modal operator convergence

- [ ] Deploy frontend bundle; confirm `requirement-modal-operator-review` on staging deeplink
- [ ] Live verify/reject from modal with queue row removal (non-destructive test org)
- [ ] Optional: deprecate duplicate queue-row Verify/Reject once modal path validated in ops
- [ ] Platform escalation queue remains separate (no org verify in modal)
