# Rent KPI alignment watchlist

- [ ] Deploy frontend + backend with `attention_period_count` before browser proof on production bundle markers
- [ ] If upcoming total exceeds 200 consistently, consider server-side default sort + "load more" (not pagination UI yet)
- [ ] Monitor avg late payment empty state on accounts with on-time payments only
- [ ] Legacy property-group arrears row (`property:` key) still appears when tenancy_id missing
