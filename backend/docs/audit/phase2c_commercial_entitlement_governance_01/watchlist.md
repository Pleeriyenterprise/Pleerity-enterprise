# Watchlist — Phase 2C Commercial Entitlement

- Register `commercial_entitlement_expiry` on the production scheduler (daily).
- Complete staging browser scenarios A–F and populate `browser_runtime.json`.
- Migrate legacy pilot waiver / extend controls to Commercial Controls when ops sign off.
- Stripe v2: only after explicit billing programme approval (pause_collection, subscription orchestration).
- Confirm `COMMERCIAL_ENTITLEMENT_EVENT_TYPES` deployed with notification orchestrator for pre-provisioned clients.
