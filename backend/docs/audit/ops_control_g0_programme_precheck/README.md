# G0 bundle template

Runtime bundles are created at:

`ops_control_g0_programme_precheck_{client_id}_{property_id}/`

Generate scaffold artifacts (NOT_EXECUTED) via:

`python tmp_ops_control_g0_programme_precheck_execute.py`

Use `--execute-runtime` only when intentionally running G0 verification against staging.
