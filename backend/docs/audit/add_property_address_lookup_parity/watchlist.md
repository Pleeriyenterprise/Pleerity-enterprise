# Watchlist

1. **Browser E2E** — Run manual checklist in `REPORT.md` on staging; reclassify to `VERIFIED_OPERATIONALLY` when dashboard postcode dropdown + create + requirements confirmed.
2. **Invalid postcodes** — Lookup 404 leaves manual path; no user-facing regression expected.
3. **Intake regression** — Re-run `IntakePage.test.js` in CI on merge (shared hook refactor).
